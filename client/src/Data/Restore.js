export const URL = "http://localhost:5000";

export var update = {
    state: false,
    time: "no support"
}

export var user = {
    Room: null,
    name: null,
    windowSize: { height: window.innerHeight, width: window.innerWidth }
}

export var map = {
    kind: null,
    size: null,
    data: null
}